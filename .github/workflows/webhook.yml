name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: List Playwright tests
        run: npx playwright test --list

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

            - name: Notify Microsoft Teams (via curl)
        if: always()
        env:
          TEAMS_WEBHOOK: ${{ secrets.MS_TEAMS_WEBHOOK_URI }}
        run: |
          status="${{ job.status }}"
          color="FFA500"
          emoji="🟠"
          if [ "$status" = "success" ]; then
            color="00FF00"
            emoji="✅"
          elif [ "$status" = "failure" ]; then
            color="FF0000"
            emoji="❌"
          fi

          json=$(cat <<EOF
{
  "@type": "MessageCard",
  "@context": "https://schema.org/extensions",
  "themeColor": "$color",
  "summary": "Résultat des tests Playwright",
  "sections": [{
    "activityTitle": "$emoji Playwright Tests – $status",
    "facts": [
      { "name": "📦 Repository", "value": "$GITHUB_REPOSITORY" },
      { "name": "🔁 Branch", "value": "$GITHUB_REF_NAME" },
      { "name": "👤 Déclenché par", "value": "$GITHUB_ACTOR" },
      { "name": "📊 Statut", "value": "$status" }
    ],
    "markdown": true
  }],
  "potentialAction": [{
    "@type": "OpenUri",
    "name": "Voir le workflow GitHub",
    "targets": [
      { "os": "default", "uri": "https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" }
    ]
  }]
}
EOF
)

          echo "📤 Envoi de la notification à Teams..."
          curl -H "Content-Type: application/json" -d "$json" "$TEAMS_WEBHOOK"